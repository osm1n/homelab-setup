apiVersion: v1
kind: Secret
metadata:
  name: cinder-volume-rbd-keyring
  namespace: openstack
type: Opaque
data:
  # Ceph key for client.cinder user (just the key value, not full keyring)
  key: <base64-encoded-ceph-cinder-keyring>
