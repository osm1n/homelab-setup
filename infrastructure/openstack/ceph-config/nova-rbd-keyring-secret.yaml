apiVersion: v1
kind: Secret
metadata:
  name: nova-rbd-keyring
  namespace: openstack
type: Opaque
data:
  # Ceph key for client.nova user (just the key value, not full keyring)
  key: <base64-encoded-ceph-nova-keyring>
