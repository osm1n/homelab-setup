apiVersion: v1
kind: Secret
metadata:
  name: glance-rbd-keyring
  namespace: openstack
type: Opaque
data:
  # Ceph key for client.glance user (just the key value, not full keyring)
  key: <base64-encoded-ceph-glance-keyring>
